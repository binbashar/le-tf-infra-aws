#
# Backend Configuration
#

# AWS Profile (required by the backend but also used for other resources)
profile         = "bb-root"

# S3 bucket
bucket          = "bb-root-org-terraform-state-storage-s3"

# AWS Region (required by the backend but also used for other resources)
region          = "us-east-1"

# Encrypt bucket?
encrypt         = true

# DynamoDB Table Name
dynamodb_table  = "bb-root-org-terraform-state-lock-dynamo"
